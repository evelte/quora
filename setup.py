from setuptools import setup

setup(name='quora',
      version=1.0,
      url='http://quora.org',
      description='',
      author='',
      license='',
      packages=[],
      zip_safe=False,
      install_requires=[])
